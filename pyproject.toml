[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hexlet-code"
version = "0.1.0"
description = "Add your description here" # Оставил твое описание
readme = "README.md"
requires-python = ">=3.12" # Оставил твою версию Python
dependencies = []

[project.scripts]
gendiff = "hexlet_code.scripts.gendiff:main"

# --- ДОБАВЛЕННАЯ СЕКЦИЯ ---
# Настройки для сборки wheel-файла с помощью Hatch.
# Явно указываем, какую директорию с пакетом ('hexlet_code') включить в сборку.
[tool.hatch.build.targets.wheel]
packages = ["hexlet_code"]
# -------------------------